services:
  - type: web
    name: nova
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn nova_app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-4.1-mini
      - key: DEBUG
        value: false