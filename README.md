# Decision Arbitration Engine

A policy-driven system that evaluates multiple agent reports, enforces safety rules, applies human overrides, and logs decisions for auditability.

## How to Run

1. Open this folder in VS Code.
2. Run `python arbitration_engine.py`.
3. Check the logs folder for output.
